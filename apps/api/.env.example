# Leave empty for deterministic mock mode (works offline).
# Set OPENAI_API_KEY to enable multimodal LLM extraction.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
CLEARANCE_MODE=mock
DATABASE_URL=sqlite+aiosqlite:///../../data/clearance.db
UPLOAD_DIR=../../data/uploads
CONFIDENCE_HITL_THRESHOLD=0.85
